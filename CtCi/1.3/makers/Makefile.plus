
executable = app

output = ./bin/

sources = $(wildcard *.cpp)

objects = $(patsubst %.cpp,$(output)%.o,$(sources))

$(output)$(executable) : $(objects)
	@mkdir -p $(output)
	g++ -std=c++11 -g $(objects) -o $(output)$(executable)

$(output)%.o : %.cpp
	@mkdir -p $(output)
	g++ -std=c++11 -g -c $< -o $@

clean : 
	rm -fdr $(output)

#include "rotate.h"
#include <vector>

int rc(int row, int column, int dim) {
    return (row * dim) + column;
}

std::vector<int> rotateSquareMatrix(const std::vector<int>& values, int rows) {

    std::vector<int> rotated = values;

    


    return rotated;
}
